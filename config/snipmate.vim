""" Keybindings
"""""""""""""""

" Use <Leader>s to expand snippet
"nn <Leader>sr <esc>:exec ReloadAllSnippets()<cr>

let g:snips_trigger_key = '<Leader>s'

let g:snips_author = expand($NAME)
let g:author = expand($NAME)
let g:snips_email = expand($EMAIL)
let g:email = expand($EMAIL)
let g:snips_github = expand($NAME)
let g:github = expand($NAME)
